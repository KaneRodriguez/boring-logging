import React from 'react';

const LandingPage = () =>
  <div>
    <h1>Landing</h1>
    <p>The Landing Page is open to everyone, even though the user isn't signed in.</p>
    <p>This will be the page that everyone sees when they first navigate to the site. Plan accordingly.</p>
  </div>

export default LandingPage;
